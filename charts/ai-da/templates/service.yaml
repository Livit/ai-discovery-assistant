apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-da.fullname" . }}
  labels:
    {{- include "ai-da.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-da.selectorLabels" . | nindent 4 }}
